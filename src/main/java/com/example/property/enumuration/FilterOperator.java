package com.example.property.enumuration;

public enum FilterOperator {
    EQUAL,
    LIKE,
    IN,
    GREATER_THAN,
    LESS_THAN,
    BETWEEN,
    JOIN,
    TWO_JOIN,
    THREE_JOIN,
    FOUR_JOIN
}
